<div class='center'>
  <%= go_home_button%>
</div>
<p>提出された時間割の「暇」の数の合計を表示しています</p>
<p><%= @timetables.length%> 人提出済み</p>
<% row = @times_str.length%>
<% col = @dates_str.length%>
<div class="timetable">
  <div class="row">
    <div class="date"></div>
    <% @dates_str.each do |d| %>
      <div class="date"><%= d%></div>
    <% end %>
  </div>
  <% row.times do |i| %>
    <div class="row">
      <div class="time"><%= @times_str[i]%></div>
      <% col.times do |j|%>
        <div class="subject">
          <div class='distribution' id="<%=("subject" + (i * col +  j).to_s)%>"></div>
        </div>
      <% end %>
    </div>
  <% end %>
</div>
<%=render partial:'group'%>
<div id='row-col-num' data-json=<%={'row' => row, 'col' => col}.to_json%>></div>
<div id='iso-url' data-json=<%= asset_url("iso.png").to_json%>></div>
<div id='hima-url' data-json=<%= asset_url("hima.png").to_json%>></div>
<div id='timetables' data-json='<%=@timetables.to_json%>'></div>
<div id='party' data-json='<%=@party.to_json%>'></div>
<div id='changeable' data-json='<%=@changeable%>'></div>
<%= javascript_pack_tag 'timetables/distribution'%>
