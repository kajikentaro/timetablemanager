<%= form_with(model: tmp) do |form| %>
  <% if tmp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tmp.errors.count, "error") %> prohibited this tmp from being saved:</h2>

      <ul>
        <% tmp.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
